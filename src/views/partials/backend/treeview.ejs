<%
var markup = '';
function htmlTreeBuilder(items) {
    if (items.length) {
        markup += '<ul>';
    }
    for (var i = 0; i < items.length; i++) {
        markup += '<li>';
        markup += '<div class="icon-name-group">' + items[i].name + '<span class="icon-group"><span class="btn mdi text-secondary mdi-playlist-edit"></span><span class="btn mdi mdi-delete text-secondary"></span></span></div>';

        // Children
        htmlTreeBuilder(items[i].children);

        markup += '</li>';
    }
    if (items.length) {
        markup += '</ul>';
    }
}
htmlTreeBuilder(tree);
%>

<%- markup %>